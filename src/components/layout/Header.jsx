import headerStyle from "src/styles/layout/_header.module.scss";

const Header = () => {
    return (
        <header className={headerStyle.header}>
            Header Content
        </header>
    );
}

export default Header;